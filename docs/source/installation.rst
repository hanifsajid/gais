Installation
============

Install via pip:

.. code-block:: bash

    pip install gais


Or from source:

.. code-block:: bash

    git https://github.com/hanifsajid/gais.git
    cd gais
    pip install -e .
